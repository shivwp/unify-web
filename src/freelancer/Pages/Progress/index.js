import Progress from "./components/progress";
import Layout from "../../Layout/Layout";

const Screen = () => {
  return (
    <Layout>
      <Progress />
    </Layout>
  );
};
export default Screen;
